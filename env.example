# Session Viewer Configuration

# Path to Claude Code sessions directory
CLAUDE_DIR=${HOME}/.claude

# Path to Codex sessions directory
CODEX_DIR=${HOME}/.codex

# Directories containing your projects (comma-separated)
# These are mounted read-only so the viewer can display project .md files
PROJECT_DIRS=${HOME}/Documents/Development

# Port to expose the web UI on
PORT=3000

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
